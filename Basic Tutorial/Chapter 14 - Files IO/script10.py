# The mkdir() Method

# You can use the mkdir() method of the os module to create directories in the
# current directory. You need to supply an argument to this method which contains the name
# of the directory to be created.

import os

# Create a directory "test"
os.mkdir("test")